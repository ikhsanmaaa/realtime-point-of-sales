import PaymentStatus from "../_components/payment-status";

export const metadata = {
  title: "Cafe APP | Failed-Payment",
};

export default function FailedStatusPage() {
  return <PaymentStatus />;
}
